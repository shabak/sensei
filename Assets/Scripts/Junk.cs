namespace Assets.Scripts
{
    public class Junk
    {
//            if (Time.realtimeSinceStartup > 0.5) {
//            if (acc[i] > 150) {
//                if (Vector3.Distance(item.position, new Vector2(item_.x, item_.y)) < 0.04f) {
//            }

//            Debug.Log(Time.realtimeSinceStartup);
//            Debug.Log(acc += angle);
//            Debug.Log(v[i].x + ";" + v[i].y);
//            Debug.Log(q.x + ";" + q.y + ";" + q.z + ";" + q.w);
//            Debug.Log(q.eulerAngles);
    }
}